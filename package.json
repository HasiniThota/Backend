{
  "name": "HasiniThota",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
  "start": "json-server --host 0.0.0.0 --port $PORT --watch db.json",
  "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HasiniThota/Backend.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/HasiniThota/Backend/issues"
  },
  "homepage": "https://github.com/HasiniThota/Backend#readme",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
